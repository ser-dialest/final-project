import React, { Component } from  "react";
import Layout from "../Layout";
import "./style.css";

class Game extends Component {
    render() {
        return (
            <Layout></Layout>
        );
    }
};

export default Game;