import React, { Component } from  "react";
import Layout from "../components/Layout";

class Game extends Component {
    render() {
        return (
            <Layout></Layout>
        );
    }
};

export default Game;